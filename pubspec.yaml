name: extentions
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '2.16.2'

dependencies:
  flutter:
    sdk: flutter
  dartz: ^0.10.1
  intl: ^0.17.0
  cloud_firestore: ^3.1.11


dev_dependencies:
  flutter_test:
    sdk: flutter

  lint: ^1.8.2

flutter:

  uses-material-design: true
